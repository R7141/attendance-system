FROM node:20-alpine AS builder
WORKDIR /app
COPY 网页管理端/package.json 网页管理端/package-lock.json ./
RUN npm ci
COPY 网页管理端/ ./
RUN VITE_API_BASE_URL='' npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY 后端/nginx-admin.conf /etc/nginx/conf.d/default.conf
